apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "model_manager.fullname" . }}-test-connection"
  labels:
    {{- include "model_manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "model_manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
