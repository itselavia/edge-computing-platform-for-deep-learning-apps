apiVersion: v1
kind: Service
metadata:
  name: {{ include "tflite-inference.fullname" . }}
  labels:
    {{- include "tflite-inference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tflite-inference.selectorLabels" . | nindent 4 }}
